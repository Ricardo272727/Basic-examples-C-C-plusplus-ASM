#include <stdio.h>

struct info{
	long int matricula;
	float promedio;
	char nombre[100];
	char carrera[5];
};



int main(int argc, char *argv[]) {
	/*
	r
	w (escribirlo lo crea)
	a append
	r+ lect and escri
	w+ crea lect escr
	a+ lee todo o esribe al final del mismo
	*/
	//leer los datos del archivo (obtener el numero de alumnos de cada carrera y ademas meter las matriculas en un arreglo)
	
	//pedir los datos y buscar entre las matriculas para que no se repitan
	
	//actualizar parte de estado de los registros con el numero nuevo de alumnos y el nuevo promedio de cada carrera
	
	
	
	
	
	return 0;
}

